# Clerk Authentication (Required for both apps)
# Get these from https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_publishable_key
CLERK_SECRET_KEY=sk_test_your_secret_key

# App URLs (Optional - uses smart defaults)
# Development defaults:
# - Landing: http://localhost:3100
# - App: http://localhost:3000
# Production defaults:
# - Landing: https://www.accessaurus.com
# - App: https://app.accessaurus.com

# Override if using different URLs:
# NEXT_PUBLIC_LANDING_URL=https://www.accessaurus.com
# NEXT_PUBLIC_APP_URL=https://app.accessaurus.com

# Clerk URLs Configuration (Optional - uses app defaults)
# These are automatically configured based on the app type
# Landing page sign-in/up redirects to app dashboard
# App dashboard sign-out redirects to landing page
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/

# Sanity CMS (Required for landing page blog)
# To set up Sanity:
# 1. Run: npm create sanity@latest -- --env=.env.local --create-project "Accessaurus Blog" --dataset production
# 2. Import demo data (optional): npx sanity@latest dataset import seed.tar.gz
# 3. Manage content at: http://localhost:3100/studio
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_READ_TOKEN=your_read_token  # Optional - for draft mode
